<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.bookandmovie.Dao.PostDao">
    <select id="findPost" parameterType="int" resultType="Post">
        select * from post where pid = #{pid};
    </select>
    <insert id="addPost" parameterType="Post">
        insert into post(username, date, content, thumbs, response, transmit, length)
        values(#{username}, #{date}, #{content}, #{thumbs}, #{response}, #{transmit}, #{length});
    </insert>
</mapper>